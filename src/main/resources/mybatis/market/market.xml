<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.cojam.web.dao.MarketDao">

    <insert id="saveMarket" parameterType="io.cojam.web.domain.Market" >
        INSERT INTO TB_MARKET
        (
            MARKET_KEY, SEASON_CATEGORY_KEY, MARKET_TITLE, FILE_KEY, SNS_URL, SNS_ID, CREATOR_ADDRESS, MARKET_DESC, MARKET_STATUS, CREATE_MEMBER_KEY, SEASON_KEY
        )
        VALUES
        (
            #{marketKey}, #{seasonCategoryKey}, #{marketTitle}, #{fileKey}, #{snsUrl}, #{snsId}, #{creatorAddress}, #{marketDesc}, #{marketStatus}, #{createMemberKey}, #{seasonKey}
        )
    </insert>

    <insert id="saveMarketAnswer" parameterType="io.cojam.web.domain.MarketAnswer" >
        INSERT INTO TB_MARKET_ANSWER
        (
            MARKET_ANSWER_KEY, MARKET_KEY, MARKET_EXAMPLE, ANSWER_TITLE
        )
        VALUES
        (
            #{marketAnswerKey}, #{marketKey}, #{marketExample}, #{answerTitle}
        )
    </insert>
</mapper>